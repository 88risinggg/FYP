# Client Workspace

React, Vite, and Tailwind CSS frontend for the FYP dashboard.

Script entry points:

- `npm run dev` starts the Vite development server.
- `npm run build` creates the production frontend bundle.
- `npm run preview` serves the built bundle locally.

The Finance invoicing views live under `src/pages/invoicing/FinanceInvoicingPage.jsx`.
