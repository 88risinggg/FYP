# Server Workspace

Node.js, Express, and mysql2 backend for the FYP dashboard.

Script entry points:

- `npm run dev` starts the API with `nodemon src/server.js`.
- `npm start` starts the API with `node src/server.js`.

The Express app is composed in `src/app.js`, and invoice-related routes are registered under `/api`.
